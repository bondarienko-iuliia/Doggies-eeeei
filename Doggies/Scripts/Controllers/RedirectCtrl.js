webApp.controller("RedirectCtrl", ["$rootScope", "$scope", "$routeParams", "$location", function ($rootScope, $scope, $routeParams, $location ) {
   
    //$rootScope.showActualEvents = true;
    //$scope.$watchGroup(["actualEvents", "eventId"], function (values) {
    //    //console.log("!!!");

    //    // редирект, только если есть актуальные события
    //    if (values[0] && values[0][0]) {
    //        security.checkUserIsAuthorized().then(function () {
    //            $location.path("/home/index");
    //        });
    //    }
    //}, true);
}]);